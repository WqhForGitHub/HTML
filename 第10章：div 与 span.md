# 仔细观察饮料 HTML
![仔细观察饮料 HTML](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/Head%20First%20HTML%20%E4%B8%8E%20CSS%EF%BC%88%E7%AC%AC%E4%BA%8C%E7%89%88%EF%BC%89/%E7%AC%AC10%E7%AB%A0%EF%BC%9Adiv%20%E4%B8%8E%20span/%E4%BB%94%E7%BB%86%E8%A7%82%E5%AF%9F%E9%A5%AE%E6%96%99HTML.png)
# 下面来研究如何将一个页面划分为逻辑区

来看右边的 Web 页面，这是为 PetStorz.com 建立的一个 Web 页面，接下来几页我们会研究如何为这个页面增加一些结构，首先找出一些逻辑区，然后把这些逻辑区分别包围在一个 `<div>` 元素照中。
![找出逻辑区](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/Head%20First%20HTML%20%E4%B8%8E%20CSS%EF%BC%88%E7%AC%AC%E4%BA%8C%E7%89%88%EF%BC%89/%E7%AC%AC10%E7%AB%A0%EF%BC%9Adiv%20%E4%B8%8E%20span/%E9%A1%B5%E9%9D%A2%E5%88%92%E5%88%86%E4%B8%BA%E9%80%BB%E8%BE%91%E5%8C%BA.png)
## 找出逻辑区

OK，所以你的任务就是找出这个页面中的逻辑区（logical section）。什么是逻辑区？逻辑区就是页面上彼此相关的一组元素。例如，在 PerStorez.com 的 Web 页面上，有一些元素用于猫猫区（cats），另外一些用于狗狗区（dogs）。下面来看一下。
	![找出逻辑区](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/Head%20First%20HTML%20%E4%B8%8E%20CSS%EF%BC%88%E7%AC%AC%E4%BA%8C%E7%89%88%EF%BC%89/%E7%AC%AC10%E7%AB%A0%EF%BC%9Adiv%20%E4%B8%8E%20span/%E6%89%BE%E5%87%BA%E9%80%BB%E8%BE%91%E5%8C%BA.png)a

既然已经知道各个逻辑区包括哪些元素，下面可以增加一些 HTML 来标记这个结构，这有一种常用的方法：在属于一个逻辑区的元素周围放置 `<div>` 开始和结束标记。下面先用图解做这个工作，后面几页再完成真正的标记。
>问：这么说，`<div>` 就相当于一个容器，可以把元素一同放在这个容器里，是吗？
>答：没错。实际上，我们通常就把 `<div>`描述为容器。他们不仅相当于逻辑容器，可以用来将一堆相关的元素（如 "cat" 元素）放在一起吗，另外对 `<div>` 指定样式和定位时（见下一章，你会看到他们还相当于图形容器）
>
>问：页面中除了用标题和段落等建立的结构外，我还应用用 `<div>` 增加更高一层的结构，是吗？
>答：这个不好说，如果确实有实际作用，就可以更加结构，不过不要为了了加结构而不必要得增加结构，一定要在保证完成任务的前提下让结构尽可能简单，例如，如果为 PetStorz 页面增加一个 pets区包含 cats 和 dogs 区会有帮助，就要尽可能增加这层结构，不过，如果它不能提供任何实际的好处买，就只会让页面更复杂，
>使用 `<div>` 一段时间后，你就会对什么时候使用以及使用多少 `<div>` 才合适有点感觉了。
>
>问：你把 `<div>` 放在一个类里，而不是为它指定一个 id，这样行吗？
>答：嗯，要记住群，一个元素有一个 id，同时可以属于一个或多个类，所以这个选择并不互相排斥。没错，很多情况在下会创建 `<div>` 并把它们放在类中，假设你的音乐播放列表页面中有很多唱片区，你可以把组成唱片的所有元素放在一个 `<div>`中，然后把这些 `<div>` 放在一个 albums 类中，这样可以标识唱片在哪里，而且可以利用类对它们同时指定样式，不仅如此，你还可以为每个唱片制定一个唯一的id，从而能为它单独的应用额外的样式。
>sdf
>问：我对 `<div>` 嵌在 `<div>` 中的做法还不太明白，比如 "pets"、"cats" 和 "dogs" 之间的关系，这到底是怎么回事。你能再做些解释吗？
>sdf
>答：当然可以。你习惯于元素嵌套在其他元素中，对吗？比如一个 `<p>` 嵌套在一个 `<body>` 中，而 `<body>` 又嵌套在一个 `<html>` 元素中。你甚至见过列表嵌套在另一个元素中。对于 PetStorz 页面，我们使用 `<div>` 来显示更大的结构块（"cats" 和 "dogs" 嵌套在一个 "pets" 区）。或者，也可以使用 `<div>` 将一个啤酒区嵌套在饮料区中，而饮料区进一步嵌套在酒单区中。
>不过，要理解为什么需要像这样嵌套 `<div>`，最好的办法是在遇到合适的情况时具体使用 `<div>`。记着这一点，稍后就会看到一个需要嵌套 `<div>` 的例子。

**在页面中要使用 `<div>`，但不要滥用。如果这样做有助于你将页面分解为逻辑区，从而保证结构清晰并便于指定样式，那么可以增加更多的结构。如果只是为了在页面中创建大量结构而增加 `<div>`，就只会让页面复杂，而没有任何实际好处**。

